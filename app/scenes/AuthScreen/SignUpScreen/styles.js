import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    backgroundColor: '#fff'
  },
  button: {
    borderColor: '#48b',
    backgroundColor: '#49c'
  },
  textStyle: {
    color: '#fff'
  }
});

export default styles;
